# ─── OS / editor clutter ───────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
*.swp
*.swo
.idea/
.vscode/
*.sublime-workspace

# ─── Chrome package artefacts ─────────────────────────────────────────────────
# The .crx is the signed package you upload; never commit it — generate on demand.
*.crx
*.pem          # Private key used to sign the extension — NEVER commit this.

# ─── Build / export folders ───────────────────────────────────────────────────
dist/
build/
release/

# ─── Theme switching artefact ─────────────────────────────────────────────────
# The helper script writes a marker when dark mode is active.
# Never commit it — manifest.json must stay as the light source of truth.
.manifest.active

# ─── Secrets ──────────────────────────────────────────────────────────────────
.env
secrets.*
